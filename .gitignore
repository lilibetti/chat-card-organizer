# --- Data ---
*.db
*.db-journal
*.sqlite
*.sqlite3

# --- Generated / personal data ---
# `02-cards.json` and `04-labeled.json` are sample/demo files in the repo
# so a fresh clone can be served with `python 05-serve.py` immediately.
# When you generate real data locally and want to commit updated samples,
# review the diff carefully first — do NOT commit private conversations.
# Intermediate batch files from `03-label.py` are never tracked.
03-batch-*.json

# --- Backups / archives ---
*.zip
*.tar.gz
*.7z

# --- Reference / third-party sources ---
reference/
_external/
vendor/

# --- Editor / OS junk ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
.pyc
__pycache__/

# --- Local state ---
.env
.env.local
*.local
